#include <bits/stdc++.h>

using namespace std;

int f(int n) {
	vector<int> a(1);
	for (int i = 0; i < n; i++) {
		a[i] = 100;
	}
}
int *p;
int main() {
	int s;
	cin >> s;
	switch (s) {
		case 1:
			cout << "AC" << endl;
			break;
		case 2:
			cout << "WA" << endl;
			break;
		case 3:
			while (1)
				p = new int[1 << 24], p[1 << 20] = 1;
		case 4:
			while (1)
				;
			break;
		case 5:
			f(100);
			break;
		default:
			break;
	}
}