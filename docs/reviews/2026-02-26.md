# Code Review: `src/core/` — 2026-02-26

**审计范围**：16 个文件，约 3,200 行
**审计人**：Antigravity (AI)
**状态**：P0 全部已修复；以下为待处理项

---

## 待处理问题

### P1 — 短期修复

| # | 文件 | 描述 | 建议 |
|---|------|------|------|
| 1 | `contestant.h/cpp` | **getter/setter 无边界检查**：所有按 `index` 访问的方法（`getCheckJudged`、`getCompileState` 等）直接使用 `[]`，无效索引导致 UB | 在每个访问方法中添加 `Q_ASSERT(index >= 0 && index < list.size())` |

### P2 — 中长期架构改进

| # | 文件 | 描述 | 建议 |
|---|------|------|------|
| 2 | `judgingthread.cpp` | **单文件职责过重**（1,402 行）：进程管理、6 种输出比较器、Win/Linux/macOS 沙箱混杂 | 拆分为 `OutputComparator` + `PlatformProcessRunner` + `JudgingThread`（仅调度） |
| 3 | `contestant.h` | **平行数组反模式**：10 个 `QList` 并行存储每道题数据，三层嵌套 `QList<QList<QList<T>>>` | 引入 `TaskResult` 结构体，简化为 `QList<TaskResult>` |
| 4 | `contest.h:74` | **信号参数匿名**：`singleCaseFinished(QString, int, int, int, int, int, int, int)` 含 7 个匿名 `int` | 定义 `SingleCaseResult` 结构体作为信号参数 |
| 5 | `testcase.cpp:113` | **子任务依赖序列化 hack**：依赖信息编码为特殊后缀文件名混入 `inputFiles`，若文件名与后缀碰撞则数据损坏 | 在 JSON 中使用独立的 `dependenceSubtask` 字段 |
| 6 | `judgingthread.cpp:255` | **错误信息截断**：`compareLineByLine` / `compareIgnoreSpaces` 的错误消息仅展示当前 32 字符分块片段，而非完整行 | 累积完整行内容后再构造错误消息 |

---

## 已修复（本次审计期间）

| Commit | 内容 |
|--------|------|
| `d2251b6` | `skipEnabled` 改为 `std::atomic<bool>`，消除数据竞争 |
| `e0390df` | `readFromJson` 各 `clear()` 前添加 call-once 约束注释 |
| `68aa941` | `taskjudger.cpp:439` 内层循环变量 `i` 重命名为 `fileIdx` |
